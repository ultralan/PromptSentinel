[project]
name = "prompt-guard-ft"
version = "0.1.0"
description = "Prompt Guard 2 在 PromptShield 上的 LoRA 与全量微调训练骨架"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.2.0",
    "datasets>=3.2.0",
    "huggingface-hub>=0.27.0",
    "numpy>=2.0.0",
    "peft>=0.14.0",
    "pyyaml>=6.0.2",
    "safetensors>=0.4.5",
    "torch>=2.5.0",
    "transformers>=4.47.0",
]

[tool.uv]
package = false
